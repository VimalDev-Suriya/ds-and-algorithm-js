// * Problem Statement
// Generate a Number N - Generate all strings of length N, using only "A" and "B"
// Example - If N = 2, possible strings are AA, AB, BA, BB
// Example - If N = 3, AAA, AAB, AAC.... CCC (27 Strings)

const generateStrings = (n) => {};

generateStrings(2);
