# Sliding window Algorithm

How to identify these type of problems?

1. Fixed array size window, (Subarray/Substring of length K). Check [here](./constant-window.js)
    - here the K is the size of the subarray.
2. Dynamic size of the substring
3. Substring with Character frequency conditions