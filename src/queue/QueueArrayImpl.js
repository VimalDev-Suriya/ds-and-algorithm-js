/**
 * @class Queue
 *
 * Queue follows the `FIRST IN FIRST OUT` principle
 *
 * @example
 * initial arr = []
 * push(a)
 * result -> arr = [a]
 * push(b)
 * result -> arr = [a, b]
 * push(c)
 * result -> arr = [a, b, c]
 * pop()
 * result -> arr = [b, c]
 */

class QueueArrayImpl {
  constructor() {}
}

module.exports = {
  QueueArrayImpl,
};
